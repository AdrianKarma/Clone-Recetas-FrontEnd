import { Container } from "react-bootstrap";

const Inicio = () => {
    return (
        < >
        <Container fluid className="mainSection">
            <h1>Main</h1>
            </Container>
        </>
    );
};

export default Inicio;